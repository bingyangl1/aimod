{
  "version": "1.0.51",
  "build": "2026-05-26-r62",
  "fixes": [
    "P0: ACTION_TYPE_ALIASES — LLM-generated action types (place/placeBlock/break etc) mapped to standard names",
    "P1: incrementalReplan empty-acts handling — consecutiveUnknown counter, skip after 2, FAIL after 3",
    "P2: WorldObservation dedup — isDuplicateOf() skips identical observations when bot is idle",
    "P3: UnstuckDetector paused during LLM replan — prevents false WAIT/JUMP cycling",
    "P4: UTF-8 arrow fix — logs->4 planks (ASCII) instead of logs→4 planks (Unicode corruption)",
    "P5: ContextAssembler replan history — injects recent failed attempts so LLM doesn't repeat them",
    "P6: LLMService.buildPrompt() — explicit action type name enforcement in Response Format"
  ]
}
