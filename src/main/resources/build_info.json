{
  "version": "1.1.18",
  "build": "2026-06-26-r118",
  "fixes": [
    "r65: deferred PlanCache store — only cache plans after task COMPLETED (not at parse time)",
    "r65: PlaceBlock canBeReplaced — snow/grass no longer block placement",
    "r66: PlaceBlock placement direction fix — click solid face, not air (Baritone approach)",
    "r66: PlaceBlock fallback to direct setBlock when useItemOn fails for FakePlayer",
    "r67: global position array/object flattening in parseActionFromJson — move_to no longer goes to (0,0,0)",
    "r67: position supports both array [-530,173,-132] and object {x:-531,y:173,z:-129} formats",
    "r68: TaskPersistence — in-progress tasks saved to config/aimod/tasks/<uuid>.json, auto-restored on server restart",
    "r68: persistTasks config option (default true) — disable to skip task persistence",
    "r69: Bot task status displayed above head — name (green) + state + task + progress, showTaskAboveHead config",
    "r70: Name tag works in fp mode — FakePlayer.getDisplayName() now shows task status (shared NameTagFormatter)",
    "r70: GatherResourceAction infinite loop fix — max 10 target skips, clear stale failedTargets on radius expand",
    "r70: A* consecutive fail fast-give-up (5 fails → skip target), ObstacleBreaker scans all 6 directions",
    "r71: Full bot inventory GUI — armor (4) + offhand + main inventory (27) + hotbar (9) = 41 slots, vanilla inventory texture",
    "r71: Custom BotInventoryMenu with ModMenuTypes registration, BotInventoryScreen with vanilla inventory texture",
    "r72: DefenseChain cooldown on target-death and retreat — prevents re-triggering every 2s when no real threat",
    "r72: BotAIStateMachine transitions — startPlanning, actionCompleted, complete called at correct lifecycle points",
    "r72: Safety IDLE reset in FakePlayer.tick — state machine no longer stuck at EXECUTING after task completion",
    "r73: FakePlayer name tag fix — setCustomName metadata sync + RenderNameTagEvent handler replaces profile name with task status",
    "r74: PlaceBlock findBlockItem expanded to slot 0-40 (was 0-35, missed armor/offhand), distance check > 6 blocks",
    "r74: Replan unknown action feedback — raw LLM output added to Recent Failed Attempts so LLM can fix format errors",
    "r74: UnstuckChain cancel reason — sends translatable message before cancelling task (stuck 15s+)",
    "r75: Name tag shows current step — [x/y stepDesc] taskDesc format, BotAIStateMachine tracks currentActionDesc",
    "p2a: GatherResourceAction findBestStandPos dy range -2~+3 → -10~+3, findAdjacentStandPos adds downward search",
    "p2a: Reach check dy >= -1.5 → -3.0, MAX_FALL_BLOCKS 4 → 10 for deeper underground pathfinding",
    "p2a: LLM shorthand JSON auto-correction — {\"mine\":\"iron_ore\"} → {\"type\":\"mine\",\"block_type\":\"iron_ore\"}",
    "p2b: BotAIManager decomposition — TaskPlanner (LLM+parse), TaskExecutor (action loop), TaskReplanner (replan logic)",
    "p2c: BotCommand decomposition — SubCommand interface + BotCommandTask/Action/Admin/Config/TestCmd/Help. BotCommand reduced to 150-line coordinator.",
    "p3a: Test coverage expansion — ActionFactoryTest, NameTagFormatterTest, InventoryUtilsTest. Total 18 test files, 150+ test methods.",
    "r76: InventoryUtils.consumeItems slot 0-40 (was 0-35, missed armor/offhand) — same bug pattern as r74 findBlockItem/findBlockSlot",
    "r76: InventoryUtils.find() armor slot check — added loop for slots 36-39 (armor) so elytra/pumpkin in armor slots are found",
    "r76: VeinMineAction AIR race condition — target removed between Phase1 and Phase2 now sets FAILED instead of infinite IN_PROGRESS",
    "p3b: Multi-model routing — CHEAP_MODEL_NAME config for replan, LLMService.sendPromptWithModel(), TaskReplanner uses cheap model",
    "p3c: BotMetrics observability — thread-safe counters for LLM calls, tasks, actions, replans. /ai_bot metrics command.",
    "p4a: Test coverage expansion — BotMemoryStoreTest, ContextAssemblerTest, TaskPersistenceTest. Total 21 test files, 170+ test methods.",
    "p4b: TaskPersistence hardening — version field, atomic write (tmp+rename), corrupted JSON backup, action index validation",
    "p4c: MovementDigDown — multi-block vertical dig-down (dy<=-2), break block below + gravity fall, safety checks for liquids/bedrock/void",
    "p5a: GatherResourceAction Strategy 4 (tryDigDown) — breaks block below to reach underground targets, safety for void/bedrock/liquids",
    "p5b: Detailed failure feedback — Action.failReason field, specific messages for equip/craft/place/break actions",
    "p5c: PlayerDefenseChain — PvP defense (priority 65), retreat + shield when attacked by non-owner players, enablePvpDefense config",
    "p6a: MovementDigDown tool validation — hasPickaxe check, getToolSpeed for break time, warn on bare-hand dig",
    "p6b: VeinMiningHelper ore support — veinMineOre() + isOreBlock(), GatherResourceAction vein mines ores when enabled",
    "p6c: ChainManager config-based registration — enableDangerChain/enableDefenseChain/enableFoodChain/enableUnstuckChain config options",
    "r77: LLMService.sendPromptWithModel thread safety — pass model through call chain instead of mutating shared state",
    "r77: FakePlayer state machine reset on null task — prevents stuck in PLANNING state",
    "r77: DefenseChain shield lowered on deactivation — stopUsingItem() called on target death, timeout, and stop()",
    "r78: TaskReplanner thread safety — AtomicInteger for counters, synchronizedList for recentReplanAttempts",
    "r78: DangerChain escape target searches Y+0~3 (was only Y+0) for underground lava escape",
    "r78: PlayerDefenseChain weapon detection uses instanceof (was fragile class name string matching)",
    "r79: MovementController pathExecutor/nextPathExecutor/directMovement made volatile for async safety",
    "r79: GatherResourceAction.breakTarget uses tick-based timing (was wall-clock System.currentTimeMillis)",
    "r79: MovementFall.update() runtime safety — fails on lava contact or Y < -64",
    "r79: GatherResourceAction.tryPillarUp suffocation check — blocks placement if head position is solid",
    "r80: TaskPlanner returns failed Task instead of null when all parsing fails",
    "r80: GatherResourceAction water escape timeout (5s) — prevents infinite loop in ocean",
    "r80: PlaceBlockAction.findBlockSlot swaps item to hotbar when found in armor/offhand slots",
    "r80: VeinMiningHelper passes bot entity to destroyBlock for proper loot table context",
    "r80: FakePlayer.tick() catches all Exception (was only NullPointerException)",
    "r81: ContextAssembler replan history respects token budget — truncates when base context is near limit",
    "r81: CraftAction output count capped to max stack size",
    "r81: MovementDigDown getToolSpeed only considers PickaxeItem (was any TieredItem)",
    "r81: PlaceBlockAction findNearbyAir returns bot position instead of occupied targetPos",
    "r81: PlanCache synchronized — find/markFailed/store use lock for thread safety",
    "r82: MineBlockAction dig-down strategy — when pathfinder fails and target is below, breaks blocks to create staircase down to underground ores",
    "r83: BotMetrics recordTaskStarted/Failed/Completed integrated into task lifecycle",
    "r83: VeinMiningHelper truncation warning when vein exceeds maxBlocks",
    "r83: MovementFall fallDistance computed in constructor instead of calculateCost()",
    "r83: TaskReplanner removed duplicate consecutiveUnknown reset",
    "r84: WorldObservation scan frequency reduced from 1s to 3s — reduces server load with multiple bots",
    "r84: ModConfig compactTriggerTokens capped to maxContextTokens-512 — prevents misconfiguration",
    "r85: DangerChain auto-retry protection — checks hasActiveTask() before re-assigning saved command",
    "r86: BotMetrics persistence — save/load to JSON file, periodic save every 5 minutes, save on bot kill",
    "r87: PlaceBlockAction optimization — findPlaceableFace returns null when no solid adjacent, skips useItemOn and goes directly to fallbackSetBlock",
    "r88: MovementStepUp — new movement type for dy=1,adx+adz=1 (step up + horizontal), fixes missing movement case in BotMovement factory",
    "r89: GatherResourceAction ToolSet integration — auto-select best tool, accurate break time with enchantments/effects",
    "r90: Test cleanup — removed tests that depend on Minecraft runtime (VeinMiningHelperTest, MovementDigDownTest, MovementStepUpTest)",
    "r91: Task.injectAction() size cap at MAX_ACTIONS=100 — prevents unbounded list growth during repeated replan loops",
    "r92: BreakBlockAction ToolSet integration — auto-selects best tool + accurate break time",
    "r92: showPath visual improvements — red target marker, progress display, yellow direct line when no path",
    "r93: LLMService health check cache race fix — ConcurrentHashMap per-model instead of single AtomicReference",
    "r94: SSE streaming response uses InputStream — reads line by line instead of loading entire body into memory",
    "r95: Bot skin system — BotSkinManager applies botSkinUrl config to GameProfile textures property",
    "r96: FakePlayerManager uses ModConfig.getMaxBots() instead of hardcoded MAX_FAKE_PLAYERS=10",
    "r96: GatherResourceAction uses parent setFailReason() — fixes field shadowing that made getFailReason() always return null",
    "r97: WorldObservation single-pass block scan — countNearbyBatched scans volume once for all 19 block types (was 19 separate scans)",
    "r97: BotMemoryStore shortTermSummaries uses ArrayDeque — O(1) removeFirst instead of O(n) ArrayList.remove(0)",
    "r98: Dead code cleanup — removed unused MovementParkour, deprecated FakeNetHandler/FakeConnection, duplicate import",
    "r99: Exception handling — 8 silent catch(Exception ignored) blocks now log warnings via DevLog",
    "r100: LLMResponseParser pre-compiled Pattern constants (was recompiling on every LLM response)",
    "r100: MovementTraverse water swimming support — bot now swims up/down/horizontal in water",
    "r100: AutoReplaceTool/AutoReplenish slot range 0-35 → 0-40 (was missing armor/offhand slots)",
    "r101: New config options — pathfinderTimeoutMs, pathfinderMaxRadius, maxVeinSize, defenseScanRadius, defenseRetreatHealth",
    "r102: MovementClimb — ladder/vine climbing movement type, BotMovement factory world-aware mode",
    "r103: New action types — UseItemAction (eat/use), DropAction (drop items), SneakAction (sneak/crouch), LookAtAction (look at position)",
    "r104: Phase 1 audit — PlanCache.invalidate() + constructor synchronized, BotMemoryStore all methods synchronized, CraftAction output count fix",
    "r105: Phase 2 audit — DangerChain bridge placement fix (relative(facing,0) → relative(facing)), FoodChain.stop() restores inventory, DefenseChain uses config values",
    "r106: Phase 3 audit — TaskReplanner posts Task mutations to main thread, BotAIStateMachine getters synchronized, cancelTask() cancels replan",
    "r107: Phase 4 audit — deficit replan resets counters + passes ownerName, LLMService.parseResponse delegates to LLMResponseParser, MovementDigDown tool durability check",
    "r108: Phase 5 audit — MovementPillar stuckTicks reset on place/jump, PlaceBlockAction re-reads stack after inventory swap, PlayerDefenseChain retreat calls bot.move()",
    "r109: Phase 6 audit — tryDigDown max depth 20, MovementFall zero horizontal velocity on landing, FakePlayer death check in tick, NameTagFormatter % escape, pickupNearbyItems throttle 20 ticks",
    "r110: P0 audit — ChainManager preemption: higher-priority chains can now interrupt lower ones (DangerChain can preempt FoodChain)",
    "r110: P0 audit — LLMResponseParser nested JSON: replaced regex with state-machine brace/bracket counter for proper nesting",
    "r110: P0 audit — SneakAction: unconditionally reset shiftKeyDown on completion (was leaving bot sneaking forever)",
    "r110: P0 audit — UnstuckDetector PILLAR: place block below feet instead of at feet (was suffocating the bot)",
    "r110: P0 audit — DefenseChain: removed double movement in melee/retreat/shield (setDeltaMovement + move = 2x speed)",
    "r110: P0 audit — PlayerDefenseChain: removed double movement in retreat (same pattern as DefenseChain)",
    "r110: P0 audit — FakePlayer.assignTask: state machine reset in catch block (was stuck in PLANNING on LLM exception)",
    "r111: P1 audit — CraftAction: loop crafting to produce count items (was only crafting 1 batch)",
    "r111: P1 audit — BotMovement factory: return null for unrecognized movements (was creating impossible Traverse)",
    "r111: P1 audit — MovementTraverse PREPPING: verify bridge placement success before transitioning to RUNNING",
    "r111: P1 audit — MovementDigDown: add overshoot check for air-gap falls (bot was falling past destination Y)",
    "r111: P1 audit — FakePlayer.assignDirectTask: call cancelReplan() before assigning new task",
    "r111: P1 audit — TaskReplanner: state machine reset in 3 failure paths (max retries, server.execute catch, no actions)",
    "r111: P1 audit — MovementController.hasArrived: add distance check (was returning true on navigation failure)",
    "r111: P1 audit — ChainManager.stopAll: clear lastActiveChain (was stale after stopAll)",
    "r111: P1 audit — UnstuckChain SKIP: stop navigation only, don't cancel task (was conflicting with DangerChain retry)",
    "r111: P1 audit — FollowAction: move navigation logic from isComplete() to execute() (was violating method contract)",
    "r111: P1 audit — BotCommandConfig.toggleFeature: actually persist the toggled value (was display-only)",
    "r111: P1 audit — ModConfig setters: clamp values to defineInRange bounds (was bypassing NeoForge range validation)",
    "r111: P1 audit — SequencePlanner: remove crafting table prerequisite for pickaxe (placeWorkbench handles it)",
    "r112: P2 audit — ChunkCache: volatile lastChunk/lastChunkKey for cross-thread visibility",
    "r112: P2 audit — LLMResponseParser: null check on message/content fields (prevents NPE on malformed responses)",
    "r112: P2 audit — CommandParser: raise fuzzy match threshold from 3 to 5 (reduces false positives)",
    "r112: P2 audit — LLMService: close InputStream in streaming error path + 1MB limit on SSE content",
    "r112: P2 audit — PacketDistributorMixin: add logging for blocked packet types",
    "r112: P2 audit — MaterialTree: stop removing items from visited set (prevents diamond dependency blowup)",
    "r112: P2 audit — FakePlayerManager: cancelTask + AutoFish.remove before kill (prevents orphaned threads)",
    "r112: P2 audit — TaskReplanner: AtomicBoolean.compareAndSet for replanning flag (prevents duplicate replan threads)",
    "r112: P2 audit — BotSkinManager: synchronized(profile) for thread-safe skin application",
    "r112: P2 audit — ChunkCache: sort entries outside synchronized block in prune() (reduces lock contention)",
    "r112: P2 audit — RecipeIndex: build into temp maps + volatile swap (prevents concurrent read seeing empty state)",
    "r112: P2 audit — MaterialSubstitute: check available slots before consuming logs (prevents item loss)",
    "r112: P2 audit — MineBlockAction/GatherResourceAction: pathfinder timeout 2s→500ms for sync calls",
    "r113: P3 audit — Remove redundant null checks in AttackAction/InteractBlockAction/BreakBlockAction/VeinMineAction",
    "r113: P3 audit — Chain stop() cleanup: DangerChain clears escapeTarget/mlgDeployed, FoodChain clears lastBot, PlayerDefenseChain clears hostilePlayer/activeTicks",
    "r113: P3 audit — Unsafe ServerLevel casts: add instanceof checks in ObstacleBreaker/MineBlockAction/GatherResourceAction",
    "r113: P3 audit — Remove dead code: LLMService.readSSEStream(String) unused method",
    "r113: P3 audit — SayAction: add canExecute guard in execute() for blank messages",
    "r113: P3 audit — UseItemAction: fail if item not found in inventory (prevents using wrong item)",
    "r113: P3 audit — PlanCache: empty string similarity 0.0→1.0 (semantically identical)",
    "r113: P3 audit — PlanCache: atomic file write via temp+rename (prevents corruption on crash)",
    "r113: P3 audit — AutoReplenish: respect getMaxStackSize() (was hardcoded to 32)",
    "r113: P3 audit — CraftAction: recipeIndexBuilt volatile for cross-thread visibility",
    "r113: P3 audit — BotPersistence: log directory creation failure instead of silently ignoring",
    "r114: Architecture iteration — TaskPlanner: reject break_block/move_to/place_block with missing x/y/z coordinates",
    "r114: Architecture iteration — TaskReplanner: filter LLM-returned actions against recentReplanAttempts (prevents duplicate failures)",
    "r114: Architecture iteration — TaskReplanner: stronger replan prompt with CRITICAL constraint and coordinate requirement",
    "r114: Architecture iteration — MineBlockAction: cap searchRadius to 128 (was unlimited, caused 63s server lag)",
    "r114: Architecture iteration — LLM prompt: add [x,y,z are REQUIRED] and [radius max 128] annotations",
    "r114: Architecture iteration — DangerChain: escape fail cooldown (3 failures → 10s cooldown instead of infinite retry)",
    "r114: Architecture iteration — UnstuckChain SKIP: mark current action as FAILED to trigger replan (was infinite loop)",
    "r115: Agentic architecture core — AgentLoop (observe→think→act→repeat), SessionLog (packet capture/replay), Goal, AgentContext",
    "r115: TaskPlanner.parseSingleAction() for single-action parsing from AgentLoop",
    "r116: Tab list enhancement — compact status icons + task progress (空闲/规划中/执行中/重规划/已暂停/完成/失败)",
    "r115: Phase 3 integration — AgenticTaskRunner bridges AgentLoop with FakePlayer",
    "r115: Config toggle useAgenticMode (default false) — switch between one-shot and agentic modes",
    "r115: Replay command /ai_bot replay <session-id> — offline session replay for debugging",
    "r116: Agentic fix — AgentLoop multi-layer response parsing (actions list → raw content → direct JSON fallback)",
    "r116: Agentic fix — AgentContext goal analysis: item requirements, inventory gap analysis, mining depth guide",
    "r116: Agentic fix — step-by-step thinking prompt for better LLM decision quality",
    "r117: XRay-inspired OreIndex — incremental per-chunk ore caching (scan once, query many)",
    "r117: XRay-inspired BlockChangeTracker — Mixin on Level.setBlock() for real-time ore tracking",
    "r117: XRay-inspired blacklist — skip air/stone/dirt/deepslate/gravel/sand/netherrack in scan loop",
    "r117: XRay-inspired chunk-level scanning — scan by chunk with Y-spread from bot position",
    "r117: OreIndexHolder — static accessor for per-level OreIndex instances",
    "r117: WorldScanner integrates OreIndex — query cached ores first, only scan unscanned chunks",
    "r117: MineBlockAction/GatherResourceAction wire up OreIndex for incremental scanning",
    "r118: Critical fix — sendPromptWithModel/sendPrompt preserve raw response when parseResponse fails (63% parse failure → fixed)",
    "r118: AgentLoop adds AGENT_PARSE_RESULT logging for debugging parse issues",
    "Phase1 total: 22 bugfixes across r61-r67, bot now completes 'craft stone pickaxe' tasks via replan"
  ]
}
