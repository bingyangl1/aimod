{
  "version": "1.0.51",
  "build": "2026-05-26-r63",
  "fixes": [
    "P9: injectAction now followed by advanceToNextAction — replan-injected actions actually execute instead of being buried",
    "P10: place_block position array parsing — JSON {position: [x,y,z]} now correctly extracts coordinates",
    "P3cont: UnstuckDetector.setPaused(true) now clears stuck flag + strategy state — no more false WAIT/JUMP during LLM wait",
    "P7: WorldObservation.isDuplicateOf 30s time bypass — allows 1 obs per 30s even when bot is idle",
    "P11: PlanCache.markFailed() — invalidates cached plans that caused repeated replan failures"
  ]
}
