{
  "version": "1.0.51",
  "build": "2026-05-26-r61",
  "fixes": [
    "Phase1 ContextEngine: WorldObservation structured perception replaces ad-hoc string concatenation",
    "BotMemoryStore: 3-tier memory (working Deque + short-term summaries JSON + long-term knowledge JSON)",
    "ContextAssembler: token-budget dynamic assembly (60% working + 20% summaries + 10% knowledge + 10% task)",
    "Auto-compact: triggers when estimated tokens exceed compactTriggerTokens (every 100 ticks)",
    "Persistent resource locations & explored chunks across server restarts",
    "ModConfig: maxContextTokens(32000), compactTriggerTokens(24000), maxWorkingMemory(100)",
    "LLMService: estimateTokens() + context-over-limit warning before API call",
    "/aimod status: displays MemoryStore stats (working/summaries/resources/chunks)"
  ]
}
