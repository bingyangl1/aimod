settingsEvaluated { settings ->
    settings.pluginManagement.repositories {
        maven {
            url = uri("F:/MC/local-repo")
        }
    }
}

allprojects { project ->
    project.repositories {
        maven {
            url = uri("F:/MC/local-repo")
        }
    }
}